// const userModel = require('../models/user');

module.exports = {
  async login({ request }) {
    // const data = await userModel.login();
  },

  async loginout({ request }) {

  },

  async register({ request }) {
    // const data = await userModel.register();
  },
};
